server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: if you proxy /api on the same host, uncomment and set API upstream in Coolify
  # location /api/ {
  #   proxy_pass https://zo0go8484gkscgo0o4o8o0s4.api.pbshope.in;
  #   proxy_ssl_server_name on;
  # }
}
