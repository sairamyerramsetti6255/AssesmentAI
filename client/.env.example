# Local development (Vite proxies /api to localhost:3001)
VITE_API_URL=http://localhost:3001/api

# Coolify: set at build time (overrides .env.production if set in Coolify UI)
# VITE_API_URL=https://your-api-domain.pbshope.in/api
